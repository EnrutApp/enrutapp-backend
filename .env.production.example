# ==============================================
# 🚌 EnrutApp Backend - Variables de Entorno PRODUCCIÓN
# ==============================================
# 
# Este archivo es un template para Azure App Service
# Configura estas variables en Azure Portal > Configuration > Application Settings
#

# ---------------------------------------------
# 🗄️ Base de Datos (Azure Database for PostgreSQL)
# ---------------------------------------------
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_bd?sslmode=require
# Ejemplo Azure: postgresql://enrutappadmin:Password123!@enrutappdb2025.postgres.database.azure.com:5432/postgres?sslmode=require
DATABASE_URL="postgresql://enrutappadmin:YOUR_PASSWORD@enrutappdb2025.postgres.database.azure.com:5432/postgres?sslmode=require"

# ---------------------------------------------
# 🔐 JWT (JSON Web Token)
# ---------------------------------------------
# IMPORTANTE: Genera un secreto FUERTE para producción
# Comando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="GENERA-UN-SECRET-FUERTE-AQUI-CON-EL-COMANDO-DE-ARRIBA"
JWT_EXPIRES_IN="24h"

# ---------------------------------------------
# 🌐 Servidor
# ---------------------------------------------
# Azure asigna el puerto automáticamente, usa la variable PORT
PORT=8080
NODE_ENV="production"

# ---------------------------------------------
# 📧 SMTP (Correo Electrónico)
# ---------------------------------------------
# Puedes usar SendGrid (gratis en Azure) o Gmail
# SendGrid: smtp.sendgrid.net, port 587
SMTP_HOST="smtp.sendgrid.net"
SMTP_PORT=587
SMTP_USER="apikey"
SMTP_PASS="TU-SENDGRID-API-KEY"
SMTP_FROM="EnrutApp <noreply@tudominio.com>"

# ---------------------------------------------
# 🔒 CORS (Configuración de seguridad)
# ---------------------------------------------
# URL de tu frontend en producción
FRONTEND_URL="https://tu-frontend.vercel.app"

# ---------------------------------------------
# 📝 NOTAS IMPORTANTES
# ---------------------------------------------
# 1. NO subas este archivo con valores reales a Git
# 2. Configura estas variables en Azure Portal
# 3. Azure App Service inyecta automáticamente la variable PORT
# 4. Para PostgreSQL en Azure, SIEMPRE usa sslmode=require
# 5. Genera un JWT_SECRET único y fuerte para producción
